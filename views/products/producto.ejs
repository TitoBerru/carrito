<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/productoStyle.css">
  <link rel="stylesheet" href="/css/indexNormalize.css">
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
  />
  <title>Document</title>
</head>

<body>
  <%- include('../partials/header') %>
    <main>
      <div class="container">

        <div class="cont-button">
          <!-- <div class="carrito-tu-pack">  -->
            <h2>Contas con varios PACKS pre-configurados, elegi el que mas se acomode a tu necesidad</h2>
          <!-- </div> -->
        </div>

        <div class="packs">
          <% for(let i=0; i < products.length; i++) { %>
            <div class="packUnitario">
            <li class="digital animate__animated animate__backInDown">

              <img src="/img/camiones/<%= products[i].imagen %>" alt="Camion Mudanza 1" class="imagenCamion">
              <h3>Pack Nro.<%= products[i].numeroPack %></h3>
              <span>Radio: <%= products[i].radio %>km </span> <br> <br>
              <span>Superficie: <%= products[i].superficie %> Mts2 </span> <br> <br>
              <span>Valor: $ <%= products[i].precio %> </span>

              <% if (locals.isLogged && locals.userLogged.userAdmin==1) { %>
                <div class="carrito-tu-pack">
                  <button class="carrito-pack">
                    <a href="/products/edicionProducto/<%= products[i].id %>">EDITAR</a>
                  </button>
                  </a>
                </div>
                <div class="carrito-tu-pack">

                  <form action="/products/<%=products[i].id %>?_method=DELETE" method="POST"
                    onsubmit="return confirm('Esta seguro que desea eliminarlo?');">
                    <button class="carrito-pack" type="submit" class="action-button delete">
                      ELIMINAR
                    </button>
                  </form>
                </div>
                <% } %>

                  <span><a href="/products/carrito/<%=products[i].id%>"><img src="/img/carrito1.png" alt=""
                        class="btn-precio "></a></span>
                        </li>
            </div>
          <% } %>
        </div>
      </div>
    </main>
    <%- include('../partials/footer') %>
</body>

</html>